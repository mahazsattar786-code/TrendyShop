
function adminNav(){return `<aside class="admin-nav">${logo()}<p class="admin-label">COMMAND CENTRE</p><a href="#/admin">▦ <span>Overview</span></a><a href="#/admin/products">□ <span>Products</span></a><a href="#/admin/orders">⌑ <span>Orders</span></a><a href="#/admin/sale">✳ <span>Sale settings</span></a><a href="#/" class="admin-store">↗ <span>View storefront</span></a><button data-action="admin-logout">↪ <span>Log out</span></button></aside>`;}
function adminLogin(){return `<main class="login-page"><div class="login-card">${logo()}<p class="eyebrow">PRIVATE AREA</p><h1>Welcome back.</h1><p class="muted">Sign in to manage your store.</p><form id="login-form"><label>Email<input name="email" type="email" value="admin@trendyshop.pk" required></label><label>Password<input name="password" type="password" placeholder="Enter password" required></label><button class="btn btn-dark full">Sign in <span>→</span></button><small>Demo access: use the password <b>trendy2026</b></small></form></div></main>`;}
function adminPage(section='overview'){if(!sessionStorage.getItem('trendy_admin'))return adminLogin();let content='';if(section==='overview'){const stock=db.products.reduce((a,p)=>a+p.stock,0),sold=db.products.reduce((a,p)=>a+p.sold,0),pending=db.orders.filter(o=>o.status==='Pending').length,revenue=db.orders.reduce((a,o)=>a+o.total,0);content=`<div class="admin-main"><div class="admin-top"><div><p class="eyebrow">TUESDAY, 08 SEPTEMBER 2026</p><h1>Good morning, admin.</h1></div><a href="#/admin/products/new" class="btn btn-dark">+ Add product</a></div><div class="stat-grid"><div><span>Total products</span><b>${db.products.length}</b><small>In your catalogue</small></div><div><span>Available stock</span><b>${stock}</b><small>Across all products</small></div><div><span>Items sold</span><b>${sold}</b><small>All time</small></div><div><span>Order revenue</span><b>${money(revenue)}</b><small>${db.orders.length} total orders</small></div></div><div class="admin-columns"><section class="admin-box"><div class="box-head"><h2>Recent orders</h2><a href="#/admin/orders">View all ↗</a></div>${db.orders.length?db.orders.slice(-5).reverse().map(orderRow).join(''):`<div class="admin-empty"><span>⌑</span><p>No orders yet. They’ll appear here.</p></div>`}</section><section class="admin-box"><div class="box-head"><h2>Catalogue health</h2><a href="#/admin/products">Manage ↗</a></div>${db.products.slice(0,5).map(p=>`<div class="health-row"><img src="${p.image}" alt=""><div><b>${esc(p.name)}</b><span>${p.stock?`${p.stock} in stock`:'Out of stock'}</span></div><i class="${p.stock<5?'warn':''}"></i></div>`).join('')}</section></div></div>`;}else if(section==='products'){content=`<div class="admin-main"><div class="admin-top"><div><p class="eyebrow">CATALOGUE</p><h1>Products <small>${db.products.length}</small></h1></div><a href="#/admin/products/new" class="btn btn-dark">+ Add product</a></div><section class="admin-box table-box"><div class="box-head"><h2>All products</h2><input class="admin-search" placeholder="Search catalogue" id="admin-search"></div><div class="product-table">${db.products.map(adminProductRow).join('')}</div></section></div>`;}else if(section==='orders'){content=`<div class="admin-main"><div class="admin-top"><div><p class="eyebrow">FULFILMENT</p><h1>Orders <small>${db.orders.length}</small></h1></div></div><section class="admin-box table-box"><div class="box-head"><h2>All orders</h2><select id="status-filter"><option value="">All statuses</option>${['Pending','Confirmed','Processing','Shipped','Delivered','Cancelled'].map(x=>`<option>${x}</option>`).join('')}</select></div>${db.orders.length?`<div class="order-table">${db.orders.slice().reverse().map(adminOrderRow).join('')}</div>`:`<div class="admin-empty"><span>⌑</span><p>No orders yet.</p></div>`}</section></div>`;}else if(section==='sale'){const s=db.settings.sale;content=`<div class="admin-main"><div class="admin-top"><div><p class="eyebrow">STORE SETTINGS</p><h1>Global sale</h1></div></div><section class="admin-box settings-box"><div class="sale-preview ${s.active?'active':''}"><div><span class="sale-star">✳</span><h2>${esc(s.title||'Your sale title')}</h2><p>${s.percent}% off everything</p></div><b>${s.active?'LIVE':'INACTIVE'}</b></div><form id="sale-form" class="settings-form"><label class="switch-label"><input type="checkbox" name="active" ${s.active?'checked':''}><span class="switch"></span> Sale is active</label><label>Sale title<input name="title" value="${esc(s.title)}" required></label><label>Discount percentage<input name="percent" type="number" min="0" max="100" value="${s.percent}" required></label><label>Short description<textarea name="description" rows="3">${esc(s.description)}</textarea></label><button class="btn btn-dark">Save sale settings <span>→</span></button></form><p class="settings-note">Global sale prices are calculated from original prices and override individual product discounts. Discounts never stack.</p></section></div>`;}else if(section==='new'||section==='edit'){const p=section==='edit'?db.products.find(x=>x.id===currentEdit):null;content=`<div class="admin-main"><div class="admin-top"><div><a href="#/admin/products" class="back-link">← Products</a><h1>${p?'Edit product':'Add a product'}</h1></div></div><section class="admin-box product-form-box"><form id="product-form" class="product-form" data-id="${p?.id||''}"><div class="form-grid"><label>Product name *<input name="name" required value="${esc(p?.name||'')}" placeholder="e.g. Cloud Knit Sweater"></label><label>Category *<input name="category" required value="${esc(p?.category||'')}" placeholder="Fashion, Home & Living..."></label><label class="wide">Description *<textarea name="description" required rows="4">${esc(p?.description||'')}</textarea></label><label>Original price (Rs.) *<input name="price" type="number" min="0" required value="${p?.price||''}"></label><label>Individual discount (%)<input name="discount" type="number" min="0" max="100" value="${p?.discount||0}"></label><label>Stock quantity *<input name="stock" type="number" min="0" required value="${p?.stock??''}"></label><label class="wide">Image URL *<input name="image" type="url" required value="${esc(p?.image||'')}" placeholder="https://images.unsplash.com/..."></label></div><div class="image-preview">${p?`<img src="${p.image}" alt="Preview">`:'<span>Image preview appears here</span>'}</div><label class="check-label"><input name="featured" type="checkbox" ${p?.featured?'checked':''}> Show in favourites on the home page</label><div class="form-actions"><a href="#/admin/products" class="btn btn-outline">Cancel</a><button class="btn btn-dark">${p?'Save changes':'Create product'} <span>→</span></button></div></form></section></div>`;}return layout(content,true);}
function orderRow(o){return `<div class="order-row"><div><b>${o.id}</b><span>${esc(o.name)} · ${o.items.reduce((a,x)=>a+x.qty,0)} items</span></div><strong>${money(o.total)}</strong><em class="status ${o.status.toLowerCase()}">${o.status}</em></div>`;}
function adminProductRow(p){return `<div class="product-row"><img src="${p.image}" alt=""><div><b>${esc(p.name)}</b><span>${esc(p.category)} · ${p.sold} sold</span></div><strong>${money(priceFor(p).final)}</strong><span>${p.stock} in stock</span><a href="#/admin/products/edit/${p.id}" class="row-edit">Edit</a><button class="row-delete" data-delete="${p.id}">×</button></div>`;}
function adminOrderRow(o){return `<div class="order-row admin-order"><div><b>${o.id}</b><span>${esc(o.name)} · ${new Date(o.created).toLocaleDateString('en-GB')}</span></div><strong>${money(o.total)}</strong><select data-order-status="${o.id}">${['Pending','Confirmed','Processing','Shipped','Delivered','Cancelled'].map(s=>`<option ${s===o.status?'selected':''}>${s}</option>`).join('')}</select><button class="view-order" data-view-order="${o.id}">Details</button></div>`;}
let currentEdit='';
function render(){let p=path(), html;if(p==='/')html=home();else if(p.startsWith('/product/'))html=productPage(p.split('/')[2]);else if(p==='/shop'||p.startsWith('/shop?'))html=shop();else if(p==='/checkout')html=checkout();else if(p.startsWith('/confirmation/')){const o=db.orders.find(x=>p.endsWith(x.id));html=o?confirmation(o):home();}else if(p==='/about')html=about();else if(p==='/contact')html=contact();else if(p==='/admin')html=adminPage();else if(p==='/admin/products')html=adminPage('products');else if(p==='/admin/orders')html=adminPage('orders');else if(p==='/admin/sale')html=adminPage('sale');else if(p==='/admin/products/new')html=adminPage('new');else if(p.startsWith('/admin/products/edit/')){currentEdit=p.split('/')[4];html=adminPage('edit');}else html=home();document.getElementById('app').innerHTML=html; bind();}
function toast(msg,type=''){const t=document.createElement('div');t.className=`toast ${type}`;t.textContent=msg;document.getElementById('toast-region').append(t);setTimeout(()=>t.remove(),3000);}
function add(id,qty=1){const p=db.products.find(x=>x.id===id);if(!p||p.stock<1){toast('This product is currently out of stock','error');return}const next=(cart[id]||0)+qty;if(next>p.stock){toast(`Only ${p.stock} available`,'error');return}cart[id]=next;saveCart();toast('Added to your bag');render();}
function bind(){document.querySelectorAll('[data-add]').forEach(b=>b.onclick=()=>add(b.dataset.add));document.querySelectorAll('[data-action="cart"]').forEach(b=>b.onclick=()=>{document.body.insertAdjacentHTML('beforeend',cartDrawer());bindCart();});document.querySelectorAll('.search-toggle').forEach(b=>b.onclick=()=>document.querySelector('.search-bar').classList.toggle('open'));document.querySelectorAll('[data-action="menu"]').forEach(b=>b.onclick=()=>document.querySelector('nav').classList.toggle('open'));document.querySelectorAll('[data-action="admin-logout"]').forEach(b=>b.onclick=()=>{sessionStorage.removeItem('trendy_admin');navigate('/')});document.querySelectorAll('[data-action="close-strip"]').forEach(b=>b.onclick=()=>b.parentElement.remove());document.querySelectorAll('[data-action="do-search"]').forEach(b=>b.onclick=()=>navigate('/shop?q='+encodeURIComponent(document.getElementById('global-search').value)));const s=document.getElementById('global-search');if(s)s.onkeydown=e=>{if(e.key==='Enter')navigate('/shop?q='+encodeURIComponent(s.value))};document.querySelectorAll('[data-qty]').forEach(b=>b.onclick=()=>{const id=b.dataset.id,n=Math.max(1,Math.min(db.products.find(p=>p.id===id).stock,+(document.getElementById('qty-'+id).textContent)+(b.dataset.qty==='+'?1:-1)));document.getElementById('qty-'+id).textContent=n;b.closest('.buy-row').querySelector('[data-add]').onclick=()=>add(id,n)});const cf=document.getElementById('checkout-form');if(cf)cf.onsubmit=submitOrder;const lf=document.getElementById('login-form');if(lf)lf.onsubmit=e=>{e.preventDefault();if(lf.password.value==='trendy2026'){sessionStorage.setItem('trendy_admin','1');navigate('/admin')}else toast('Incorrect password','error')};const pf=document.getElementById('product-form');if(pf)pf.onsubmit=saveProduct;const sf=document.getElementById('sale-form');if(sf)sf.onsubmit=saveSale;document.querySelectorAll('[data-delete]').forEach(b=>b.onclick=()=>{if(confirm('Delete this product?')){db.products=db.products.filter(p=>p.id!==b.dataset.delete);save();toast('Product deleted');render()}});document.querySelectorAll('[data-order-status]').forEach(s=>s.onchange=()=>{const o=db.orders.find(x=>x.id===s.dataset.orderStatus);o.status=s.value;save();toast('Order status updated')});const cat=document.getElementById('category-filter'),sort=document.getElementById('sort-filter');if(cat)cat.onchange=()=>navigate('/shop?category='+encodeURIComponent(cat.value)+(sort.value?'&sort='+sort.value:''));if(sort)sort.onchange=()=>navigate('/shop?sort='+sort.value+(cat.value?'&category='+encodeURIComponent(cat.value):''));}
function bindCart(){document.querySelectorAll('[data-action="close-cart"]').forEach(x=>x.onclick=()=>{document.querySelector('.overlay')?.remove();document.querySelector('.cart-panel')?.remove()});document.querySelectorAll('[data-cartqty]').forEach(b=>b.onclick=()=>{const id=b.dataset.id;if(b.dataset.cartqty==='0')delete cart[id];else{const n=cart[id]+(b.dataset.cartqty==='+'?1:-1);if(n<1)delete cart[id];else if(n<=db.products.find(p=>p.id===id).stock)cart[id]=n;else return toast('Stock limit reached','error')}saveCart();document.querySelector('.overlay')?.remove();document.querySelector('.cart-panel')?.remove();document.body.insertAdjacentHTML('beforeend',cartDrawer());bindCart();render()});}
function submitOrder(e){e.preventDefault();const f=new FormData(e.target),items=Object.entries(cart).map(([id,qty])=>({p:db.products.find(p=>p.id===id),qty}));if(items.some(x=>!x.p||x.qty>x.p.stock))return toast('Some items are no longer available','error');const total=items.reduce((a,x)=>a+priceFor(x.p).final*x.qty,0),order={id:'TS-'+Date.now().toString().slice(-6),name:f.get('name'),phone:f.get('phone'),email:f.get('email'),city:f.get('city'),address:f.get('address'),note:f.get('note'),total,items:items.map(x=>({id:x.p.id,name:x.p.name,qty:x.qty,price:priceFor(x.p).final})),status:'Pending',created:new Date().toISOString()};items.forEach(x=>{x.p.stock-=x.qty;x.p.sold+=x.qty});db.orders.push(order);cart={};saveCart();save();navigate('/confirmation/'+order.id);}
function saveProduct(e){e.preventDefault();const f=new FormData(e.target),v=Object.fromEntries(f.entries()),id=e.target.dataset.id,p={id:id||'p'+Date.now(),name:v.name,category:v.category,description:v.description,price:+v.price,discount:+v.discount||0,stock:+v.stock,image:v.image,featured:f.get('featured')==='on',sold:id?db.products.find(x=>x.id===id).sold:0,created:id?db.products.find(x=>x.id===id).created:new Date().toISOString()};if(id)db.products=db.products.map(x=>x.id===id?p:x);else db.products.unshift(p);save();toast(id?'Product updated':'Product created');navigate('/admin/products');}
function saveSale(e){e.preventDefault();const f=new FormData(e.target);db.settings.sale={active:f.get('active')==='on',title:f.get('title'),percent:Math.max(0,Math.min(100,+f.get('percent'))),description:f.get('description')};save();toast('Sale settings saved');render();}
window.addEventListener('hashchange',render);render();
